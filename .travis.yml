addons:
  sonarcloud:
    organization: "viniiciusconceicao-github" # the key of the org you chose at step #3
    token:
      secure: $SONARTOKEN # encrypted value of your token
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner
